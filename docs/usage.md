# Usage

Run `video-to-skill doctor` first. Analyze into a new or empty directory. Review `manifest.json` for partial stages and `preview.json` for unresolved gaps. Do not proceed to semantic generation when speech evidence is missing. Use the root `SKILL.md` workflow to research and generate the candidate.

